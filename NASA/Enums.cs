namespace NASA
{
    public  class Enums
    {
        public enum RobotDirection
        {
            N = 1,
            W = 2,
            S = 3,
            E = 4
        };

        public enum RobotMovement
        {
            L,
            R,
            M
        };

    }
}
